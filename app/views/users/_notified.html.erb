<%= form_tag notification_undo_path(@user.received_notifications
                                        .where(follower_id: current_user.id).last) do %>
    <%= submit_tag "Notified", class: "btn" %>
<% end %>
